<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:FitTrackGymApp.ViewModels"
             xmlns:models="clr-namespace:FitTrackGymApp.Models"
             x:Class="FitTrackGymApp.Views.PlansPage"
             Title="{Binding Title}"
             BackgroundColor="#121212">

    <Grid RowDefinitions="Auto, Auto, Auto, *">
        
        <!-- Add/Edit Form -->
        <Frame Grid.Row="0" BackgroundColor="#1E1E1E" CornerRadius="12" HasShadow="True" BorderColor="Transparent" Padding="15" Margin="15">
            <VerticalStackLayout Spacing="10">
                <Label Text="{Binding FormTitle}" TextColor="White" FontSize="18" FontAttributes="Bold" />
                
                <Entry Text="{Binding PlanName}" Placeholder="Plan Name" TextColor="White" BackgroundColor="#2C2C2C" />
                
                <Grid ColumnDefinitions="*, *" ColumnSpacing="10">
                    <Entry Grid.Column="0" Text="{Binding DurationInMonths}" Placeholder="Duration (Months)" Keyboard="Numeric" TextColor="White" BackgroundColor="#2C2C2C" />
                    <Entry Grid.Column="1" Text="{Binding Price}" Placeholder="Price ($)" Keyboard="Numeric" TextColor="White" BackgroundColor="#2C2C2C" />
                </Grid>
                
                <Entry Text="{Binding Description}" Placeholder="Description (Optional)" TextColor="White" BackgroundColor="#2C2C2C" />
                
                <Grid ColumnDefinitions="*, *" ColumnSpacing="10" Margin="0,10,0,0">
                    <Button Grid.Column="0" Text="Clear" Command="{Binding ClearFormCommand}" BackgroundColor="#555555" TextColor="White" CornerRadius="8" />
                    <Button Grid.Column="1" Text="Save Plan" Command="{Binding SavePlanCommand}" BackgroundColor="#4CAF50" TextColor="White" CornerRadius="8" />
                </Grid>
            </VerticalStackLayout>
        </Frame>

        <!-- Loading Indicator -->
        <ActivityIndicator Grid.Row="2" 
                           IsRunning="{Binding IsBusy}" 
                           IsVisible="{Binding IsBusy}" 
                           Color="#4CAF50" 
                           HorizontalOptions="Center" 
                           Margin="0,10" />

        <!-- CollectionView for Plans -->
        <CollectionView Grid.Row="3" ItemsSource="{Binding Plans}" Margin="15,0,15,15" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:MembershipPlan">
                    <Frame BackgroundColor="#1E1E1E" CornerRadius="12" HasShadow="True" BorderColor="Transparent" Padding="15" Margin="0,0,0,10">
                        <Grid ColumnDefinitions="*, Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding PlanName}" TextColor="White" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Price, StringFormat='${0:F2}'}" TextColor="#4CAF50" FontSize="16" FontAttributes="Bold" />
                                <Label Text="{Binding DurationInMonths, StringFormat='{0} Months'}" TextColor="#AAAAAA" FontSize="14" />
                                <Label Text="{Binding Description}" TextColor="#AAAAAA" FontSize="14" Margin="0,5,0,0"/>
                            </VerticalStackLayout>
                            
                            <VerticalStackLayout Grid.Column="1" Spacing="10" VerticalOptions="Center">
                                <Button Text="Edit" 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:PlansViewModel}}, Path=EditPlanCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#2196F3" 
                                        TextColor="White" 
                                        CornerRadius="5" />
                                <Button Text="Delete" 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:PlansViewModel}}, Path=DeletePlanCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#F44336" 
                                        TextColor="White" 
                                        CornerRadius="5" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <Label Text="No plans available." TextColor="#AAAAAA" HorizontalOptions="Center" VerticalOptions="Center" />
            </CollectionView.EmptyView>
        </CollectionView>

    </Grid>
</ContentPage>
